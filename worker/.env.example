# ========= NEAR CONFIG =========
# RPC endpoint (testnet by default)
NEAR_RPC_URL=https://rpc.testnet.near.org

# Network ID: testnet | mainnet
NEAR_NETWORK_ID=testnet

# The account that will SIGN and submit results (must exist & have keys locally)
# Example: shade-worker.testnet
WORKER_ACCOUNT_ID=mynewacc.testnet

# Path to the worker account key file (near-cli format).
# Example:
#   ~/.near-credentials/testnet/shade-worker.testnet.json
NEAR_CREDENTIALS_PATH=

# ========= CONTRACTS =========
# Deployed contract accounts
GATEWAY_CONTRACT_ID=shade-gateway.testnet
VERIFIER_CONTRACT_ID=shade-verifier.testnet

# ========= WORKER BEHAVIOR =========
# Poll interval for new jobs (ms)
POLL_INTERVAL_MS=2000

# Max jobs processed in one loop tick
MAX_JOBS_PER_TICK=3

# ========= MVP "PROOF" CONFIG =========
# In the MVP we use signature-based proof (TEE-simulated).
# This secret is used ONLY if your worker signs payloads via HMAC.
# If you sign via NEAR account keys, you can leave it empty and ignore.
PROOF_MODE=near_sign   # options: near_sign | hmac
HMAC_SECRET=

# ========= DEMO PROGRAM CONFIG =========
# MVP program id that the worker understands
DEMO_PROGRAM_ID=demo_v1

# Optional: local secret used as "private input" in demo mode
# (Real version would receive encrypted inputs, TEE, KMS, etc.)
DEMO_PRIVATE_SECRET=secret:42
DEMO_PRIVATE_SALT=demo

# ========= LOGGING =========
LOG_LEVEL=info
